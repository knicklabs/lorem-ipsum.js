language: node_js
node_js:
  - "8"

cache:
  yarn: true
  directories:
    - node_modules

install:
  - npm install --no-progress

script:
  - npm test

after_success:
  - npm run coverage

branches:
  only:
    - master
